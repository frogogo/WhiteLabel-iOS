platform :ios, '13.6'

def common_pods
  # Networking
  pod 'Alamofire', '~> 5.4'
  pod 'Kingfisher', '~> 7.0'
  # Utilities
  pod 'InputMask', '~> 6.0'
  pod 'SwiftyJSON', '~> 5.0'
end

# Partner specific targets
target 'Generic' do
  common_pods
end

target 'S-Boom' do
  common_pods
end
